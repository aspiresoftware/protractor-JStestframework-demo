### Protractor basic demo example

Prerequisite:
Python
Nodejs
Bower

Use following command one by one:
`npm install`
`bower install`
`python -m SimpleHTTPServer 8080`

After starting HTTP server use following command to test protractor test-cases.

`npm test`